name: Build and deploy merch.mt

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Validate build
        run: npm run check


      - name: Deploy to Cloudflare Workers
        if: env.CLOUDFLARE_API_TOKEN != '' && env.CLOUDFLARE_ACCOUNT_ID != ''
        run: npx wrangler deploy

      - name: Smoke test production lead delivery
        if: success() && contains(github.event.head_commit.message, '[lead-smoke-test]')
        run: |
          started_at="$(node -e 'console.log(Date.now()-5000)')"
          response="$(curl --fail-with-body --silent --show-error \
            --request POST https://merch.mt/api/lead \
            --data-urlencode "name=merch.mt launch test" \
            --data-urlencode "email=order@swaggy.agency" \
            --data-urlencode "company=SWAGGY" \
            --data-urlencode "event=Production smoke test" \
            --data-urlencode "need=Automated one-time delivery test after launch. No action needed." \
            --data-urlencode "consent=on" \
            --data-urlencode "startedAt=$started_at")"
          echo "$response"
          echo "$response" | grep -q '"ok":true'

      - name: Cloudflare credentials missing
        if: env.CLOUDFLARE_API_TOKEN == '' || env.CLOUDFLARE_ACCOUNT_ID == ''
        run: |
          echo "::warning::Build completed, but deployment was skipped because CLOUDFLARE_API_TOKEN and/or CLOUDFLARE_ACCOUNT_ID are not configured as repository secrets."
